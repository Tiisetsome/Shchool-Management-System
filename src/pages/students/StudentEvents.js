import React from 'react'
import {withRouter} from 'react-router-dom'
import Events from '../../components/Events/Events'

const StudentEvents = () => {
    return <Events/>
}

export default withRouter(StudentEvents);
