import React from 'react'
import {withRouter} from 'react-router-dom'
import Events from '../components/Events/Events'

const SchoolEvents = () => {
    return <Events/>
}

export default withRouter(SchoolEvents);
